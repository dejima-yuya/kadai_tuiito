<h1>ツイート一覧</h1>

<table>
  <tr>
    <th>ツイート内容</th>
  </tr>

<% @posts.each do |post| %>
  <tr>
    <td><%= post.content %></td>
    <td><%= link_to '詳細を確認する', post_path(post.id) %></td>
  </tr>
<% end %>
</table>

<br>

<%= link_to 'ツイートを投稿する', new_post_path %>